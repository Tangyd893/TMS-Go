# Docker

Docker Compose and middleware configuration for TMS-Go.

## Start Middleware

```powershell
docker compose up -d postgres redis rabbitmq minio
```

## Start Full Stack

```powershell
docker compose up -d
```
